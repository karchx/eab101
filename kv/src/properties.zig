const std = @import("std");
const Ring = @import("ring.zig").Ring;
const Node = @import("ring.zig").Node;

//pub fn isReflexive(ring: *const Ring, repl_factor: usize) bool {
//    if (repl_factor == 0) return false;
//    const tmp: []Node = undefined;
//    return true;
//}
